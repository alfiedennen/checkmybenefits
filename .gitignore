node_modules
dist
.cache/

# Environment variables — never commit secrets
.env
.env.*
*.local

# Terraform
infrastructure/.terraform/
infrastructure/tfplan
infrastructure/tfplan.*
*.tfstate
*.tfstate.*
*.tfvars
!infrastructure/variables.tf
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Build artifacts
*.zip

# Test payloads (may contain PII samples)
lambda-output.json
lambda-payload.json

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Blog drafts — personal, not part of the codebase
blogpost*.md

# Claude Code agent data
.claude/
